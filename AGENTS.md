The instructions for any agent working in this repository are in `CLAUDE.md`: the non-negotiable rules, the
architecture, the modes, the testing approach and the build. `openspec/specs/` is the record of what is built and
`openspec/changes/` what is planned. Read `CLAUDE.md` and the specs your change touches before touching anything;
`CLAUDE.md` wins where they seem to differ.
